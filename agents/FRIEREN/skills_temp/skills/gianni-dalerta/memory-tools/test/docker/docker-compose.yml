version: '3.8'

services:
  migration-test:
    build:
      context: ../..
      dockerfile: test/docker/Dockerfile
    volumes:
      - ../..:/app/memory-tools:ro
      - test-data:/home/testuser/.openclaw
    environment:
      - HOME=/home/testuser
      - NODE_ENV=test

volumes:
  test-data:
