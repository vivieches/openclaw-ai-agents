{
  "id": "openclaw-shortcuts",
  "name": "OpenClaw Shortcuts",
  "version": "0.2.6",
  "description": "OpenClaw plugin: local /shortcuts and /projects commands (config-driven, placeholder-safe).", 
  "configSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": { "type": "boolean", "default": true },
      "includeTips": { "type": "boolean", "default": true },
      "sections": {
        "type": "array",
        "description": "Custom help sections (configure locally; keep repo placeholder-only).",
        "items": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "title": { "type": "string" },
            "lines": { "type": "array", "items": { "type": "string" } }
          },
          "required": ["title", "lines"]
        },
        "default": []
      }
    }
  }
}
