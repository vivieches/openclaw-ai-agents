interface:
  display_name: "Ops Detector + Router"
  short_description: "Deterministic incident detection and routing"
  default_prompt: "Use $ops-detection-incident-routing to run a deterministic ops detection cycle and emit incident actions."

policy:
  allow_implicit_invocation: true
